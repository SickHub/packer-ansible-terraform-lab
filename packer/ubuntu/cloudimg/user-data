#cloud-config
# https://cloudinit.readthedocs.io/en/latest/topics/modules.html
hostname: ubuntu2204-cloudimg
system_info:
  default_user:
    name: ubuntu

resize_rootfs: true
package_update: true
package_upgrade: true
packages: []

users:
  - name: ubuntu
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    lock_passwd: false
    # echo "packer" | openssl passwd -6 -stdin
    hashed_passwd: $6$g0/WJj5hfgj0kXlg$AOPpDYcTN0QZL8xk2PI7VJlUsFTQb683mbS1yZvUZ1zjEx1KpCUP6ADkMEPOjYFUUp86KbmlFkdtO9vE8.mFQ0

ssh_pwauth: true

network:
  version: 2
  ethernets:
    # this matches a specific interface during first boot
    # i.e. requires 'cloud-init clean' to be reevaluated
    eth:
      match:
        name: enp*
      dhcp4: true
